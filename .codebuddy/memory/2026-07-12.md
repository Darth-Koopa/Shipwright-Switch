# 2026-07-12

## SOH 设置菜单中文化（本地化）

为 `soh.exe` 的设置菜单增加了简体中文支持。采用**集中式本地化**架构，避免逐个包裹上千个 UI 字符串：

- 新增 `soh/soh/SohGui/Localization.h` / `Localization.cpp`：导出 `SohGui::L(const char*)`（英文原文→简中，未命中返回原文）与 `SohGui::SetMenuLanguage(bool)`。翻译表为 `gChineseTable`（`unordered_map<string,string>`，键必须与原英文字符串完全一致，含 `\n` 和 `%d %%` 格式符）。
- 在 `Menu.cpp` 的 `MenuDrawItem` 中集中本地化：用 `displayName = L(widget.name)` 绘制所有 widget 标签；下拉框选项值（combobox `comboMap`）在绘制时复制并翻译；header/sidebar 标签在 `DrawElement` 中用 `L(...)`；搜索结果来源文本也翻译。
- 工具提示（tooltip）在**消费点**本地化：在 `UIWidgets.cpp`/`UIWidgets.hpp` 的所有 `WrappedText(options.tooltip/disabledTooltip)` 处包裹 `SohGui::L(...)`（以及 `Button` 初始化器、`RadioButton`）。这样切回英文时不会丢失原文（不在原地 mutate options）。
- `SohGui.cpp` 中把所有弹出窗口标题（如 "Mod Menu"、"Input Viewer"…）用 `L(...)` 包裹。
- 新增设置项「Menu Language」（中文「菜单语言」）下拉框，绑定 CVar `Menu.Language`（0=英文,1=中文），回调调用 `SetMenuLanguage`；`Menu::InitElement` 启动时读取该 CVar。

注意：libultraship 子模块在本工作区为空，无法在此环境编译验证；需完整拉取子模块后构建。增强/随机化/开发工具等其它菜单的**标题与侧边栏**已翻译，但各菜单内大量 widget 内容字符串仅 Settings 菜单被完整翻译，其余可按需在 `gChineseTable` 中补条目即可自动生效。

## 续：Wind Waker Style + 随机生成动态选项翻译
- 完成 **Wind Waker Style** 菜单（Cel Shading / Lights / Actor Shadows / Sky 四区块）全部标签与工具提示的中文条目。
- 完成**随机生成（Randomizer）动态选项**的翻译条目：① 全部分组/区块/侧边栏名（Logic/Access、Dungeons、Shuffles、Hints/Traps、Starting Items 及各 SECTION 名）；② 约 230 个选项名称（label，来自 `settings.cpp` 的 `OPT_U8/BOOL`）；③ 选项值串（combobox 选项，来自 `settings.cpp` 的值向量，排除代码参数 `NumOpts(...)` 及已存在的 On/Off/Vanilla/Random 等）。
- **尚未翻译**（留作后续，不阻断功能）：① 随机生成的选项**描述/工具提示**（长段英文，来自 `option_descriptions.cpp`，约 230 条，键长易错且为 hover 提示）；② 约 190 个 **Logical Tricks 技巧名称**（`settings.cpp` 的 `OPT_TRICK` 第 5 参数）及其区域/标签名（`tricks.cpp` 的 `rtAreaNames`/`rtTagNames`）。

## 续 2：完成 Randomizer 动态选项的全部文本
- 经核查 `option.cpp`（`AddWidget` / `Option::GetDescription`）与 `UIWidgets.cpp`：随机生成选项**名称**经 `MenuDrawItem` 的 `L(widget.name)` 翻译；**描述/tooltip** 经 `UIWidgets` 各控件内的 `SohGui::L(options.tooltip)` 翻译（第 181/372/602/810/1045 行等）。即 `gChineseTable` 键为**拼接后的完整字符串**（含 `\n`）。
- 本轮新增翻译条目：
  1. **Logical Tricks 区域名**（33 项，`tricks.cpp` `rtAreaNames`）+ **难度标签名**（7 项，`rtTagNames`：Novice/Intermediate/Advanced/Expert/Extreme/Experimental/Glitch）；
  2. **Tricks 技巧名**（约 183 项，剔除注释误匹配 `ShuffleDungeonsEntrances`、`,`、`// Jumps` 等）；
  3. **全部 170 条选项描述**（来自 `option_descriptions.cpp` `mOptionDescriptions`，逐块 read_file 精确拼接相邻字符串字面量后翻译，分 6 批写入 `Localization.cpp`）。
- 验证：`read_lints` 对 `Localization.cpp` 无报错（编辑为纯数据追加）。PowerShell 解析相邻字符串拼接多次遇转义问题，最终改用 `read_file` 人工逐字拼接保证键精确。
- 至此随机生成（Randomizer）菜单的**标签、选项名、值串、分组/侧边栏名、区域名、标签名、技巧名、选项描述**已全部中文化。
- 仍**不自动编译**（遵守项目规则：仅用户明确下令“编译”才构建；且 libultraship 子模块本工作区为空，需先拉全子模块）。
- 验证：仅做数据追加，`read_lints` 对 `Localization.cpp` 无报错。仍**不自动编译**（遵守项目规则：仅用户明确下令“编译”才构建）。

## 续 3：补齐散落遗漏的 UI 文本
- 用 PowerShell 脚本（提取 `AddWidget(path,"X")`、`ComboMap` 值、`WindowName("X")`、`Button("X")` 字面量，与 `gChineseTable` 键做差集）系统核查 6 个主菜单源文件：`SohMenuSettings/Enhancements/Randomizer/DevTools/Network/WindWakerStyle.cpp`。
- 结论：此前工作已覆盖绝绝大部分 label / tooltip / combobox 值（含 Wind Waker Style 全段、Randomizer 描述、Network 描述、Dev Tools、动态名如 `Enable##Sail`/`Connecting...##Sail` 等）。
- 真正缺失（已补，见 `Localization.cpp` 表尾）：
  1. Randomizer「技巧/漏洞」菜单的 8 个按钮：`Collapse All##disabled/enabled`、`Open All##disabled/enabled`、`Disable All`、`Disable Visible`、`Enable All`、`Enable Visible`；
  2. Enhancements 遗漏的 4 个分隔符/标签：`Fix Vanishing Paths`、`Play Zelda's Lullaby to Open Sleeping Waterfall`、`Skip Feeding Jabu-Jabu`、`Time of Day`；
  3. Wind Waker Style 两个 separator 文本：`WWLSepNavi`→「娜薇」、`WWLSepFairy`→「其他精灵」。
- 重新跑差集脚本：缺失归零（仅剩 `中文` 这一 map 值本身为中文，无需译）。`read_lints` 对 `Localization.cpp` 无报错。
- 临时脚本/输出文件（`_extract*.ps1`、`_missing*.txt`）已删除。仍**不自动编译**（遵守项目规则）。

## 续 4：补齐 Enhancements 后半部分 + ResolutionEditor 的 tooltip
- 用改进脚本（处理跨行字符串拼接：把 `Tooltip("..." "\n" "...")` 跨行块拼成完整串，与 `gChineseTable` 键做差集）扫描全部 7 个 SohGui 源文件。
- 发现此前工作**完全遗漏**两块区域：
  1. `SohMenuEnhancements.cpp` 第 1438–1932 行（Frogs/Lost Woods Ocarina、Forest Temple、Fishing、Extra Modes、Cheats、Save States、Beta Quest、Cosmetics/Audio/Stats/Timers 弹窗等）的全部 `Tooltip` 与部分 label；
  2. `ResolutionEditor.cpp` 全文（高级分辨率编辑器：checkbox/tooltip/combobox 值如 "Original (4:3)"、"Widescreen (16:9)"、自定义 widget 文本 "X"/"Y"/"Aspect ratio"、按钮等）—— 此前从未翻译。
  3. 另有散落自 Settings/DevTools 的 tooltip（如 "Configure what happens when starting..."、"Changes the behavior of debug file select..."、Mirrored World、Bunny Hood 等）。
- 本轮补译 **73 条**完整 tooltip/label/combobox 值（含 `\n`、引号与 `%d`/`{}` 占位符保持原样）。唯一未译项是 URL `https://github.com/HarbourMasters/sail`（链接保留原文）。
- 重新跑差集脚本：缺失归零（仅剩该 URL）。`read_lints` 对 `Localization.cpp` 无报错。
- 临时脚本/输出已删除。仍**不自动编译**（遵守项目规则）。

## 续 5：全量扫描 SohGui 目录，收尾清零
- 扩展差集脚本覆盖 `SohGui/` 下全部 13 个 .cpp（含此前未扫的 `SohGui.cpp`、`SohMenu.cpp`、`SohModals.cpp`、`ImGuiUtils.cpp`），并新增提取 `ImGui::Text/TreeNode/BulletText/Combo/Slider*`、`WindowName`、`PushStyleInput` 等上下文中的 UI 文本。
- 补译最后 5 条遗漏：`Pixel Count Presets`→「像素数量预设」、`Aspect ratio: %.2f:1`（保留占位符）、`Remove this button from the combination`→「从组合中移除该按钮」、`Requires Logic Turned On.`→「需要开启逻辑。」、`Spoiler File: %s`（保留占位符）。
- 最终差集：candidates 939，missing 4，全部为技术串（`##RandomizerSeed` ImGui 隐藏 ID、`127.0.0.1` 默认 IP、`43384` 端口、`https://github.com/HarbourMasters/sail` URL），均无需翻译。**SohGui 设置菜单 UI 文本已 100% 覆盖。**
- `read_lints` 对 `Localization.cpp` 无报错。临时脚本/输出已删。仍**不自动编译**。
- 说明（已修正）：Cosmetics/Audio/GameplayStats/TimeSplits/TimeDisplay/SohStatsWindow 这些弹窗窗口**类实际在 soh 的 `soh/soh/Enhancements/` 下**（`cosmetics/CosmeticsEditor.cpp`、`audio/AudioEditor.cpp`、`gameplaystats.cpp`、`timesplits/TimeSplits.cpp`、`TimeDisplay/TimeDisplay.cpp`、`debugger/SohStatsWindow.cpp`），**不在 libultraship**。`libultraship` 子模块只是渲染后端（Gui/InputEditor/Console/Stats 窗口），弹窗内部文本由 soh 绘制。

## 续 6：拉全 libultraship 子模块 + 翻译 5+ 弹窗内部文本
- 按用户要求 `git clone --branch wind-waker-style-cel-shading --recursive https://github.com/roborich/libultraship.git libultraship`（工作区本不是 git 仓库，只有 `.gitmodules`，故直接 clone 而非 `submodule update`）。libultraship 已就位，但其内容（Gui/InputEditor/Console/StatsWindow）不含这些业务弹窗文本。
- 经核查，业务弹窗文本在 soh `Enhancements/`。逐一提取这些弹窗内**静态英文字面量 UI 文本**（Tooltip、设置名 `.name`、CVar/Slider/Combobox 标签、SeparatorText/CollapsingHeader、ImGui::Text/Button），与 `gChineseTable` 做差集。
- 翻译策略（与既有架构一致）：
  1. **Tooltip** 已在 `UIWidgets.cpp`/`UIWidgets.hpp` 的 `WrappedText(SohGui::L(options.tooltip))` **集中包裹**，故只需把全部 `.Tooltip("...")` 字符串加入 `gChineseTable` 即自动翻译（含 Audio/Cosmetics/GameplayStats 的多行拼接 tooltip、"Sound locked"/"Sound unlocked" 条件 tooltip）。
  2. **Widget 设置名/CVar/Slider 标签**：在 `UIWidgets.cpp` 集中包裹 `ImGui::Text(label, *value)` → `ImGui::Text(SohGui::L(label), *value)`（覆盖 CVar/Slider/`.name`/String 控件，共 14 处），并把 `UIWidgets::Button(label,...)`（第174行）→ `UIWidgets::Button(SohGui::L(label), ...)`；Combobox 标签在 `UIWidgets.hpp` 的 `Combobox` 模板里 `ImGui::Text("%s", trueLabel.c_str())` → `SohGui::L(trueLabel.c_str())`（4 处）。这样设置名只需加表即可翻译。
  3. **直接 ImGui 调用**（`SeparatorText`/`CollapsingHeader`/`ImGui::Text`/`ImGui::Button` 字面量，不走 widget label 参数）：在调用点逐一包裹 `SohGui::L(...)`，并为 TimeDisplay.cpp / SohStatsWindow.cpp 补 `#include "soh/SohGui/SohGui.hpp"`。
- 本轮共向 `gChineseTable` 新增约 **110 条**弹窗翻译（Cosmetics/Audio/GameplayStats 的 tooltip、设置名、滑块标签、各弹窗静态文本如 "Window Options"/"Split List Management"/"New List Name: "/"Platform: Windows"/"Status: ..."/"No Enabled Timers..." 等）。
- `read_lints` 对 `Localization.cpp`、`UIWidgets.cpp`、两弹窗 cpp 等**全部无报错**。临时脚本/输出已删。**未自动编译**（遵守项目规则；libultraship 已拉全，如需构建需在本环境安装工具链）。

## 续 7：追踪器/输入编辑器/联机窗口收尾
- 重跑差集脚本 `_scan_missing.ps1`（键取自 `Localization.cpp`，扫源码 `ImGui::*`/`.Tooltip`/`CVar*`/`AddWidget`；跳过含 `SohGui::L(` 行）。发现之前会话已覆盖绝大部分，本轮仅剩 20 候选。
- 确认 `UIWidgets.cpp` 的 `Checkbox` 在第 365 行 `RenderText(labelPos, SohGui::L(label)...)`、第 372 行 tooltip 均自动翻译；`CVarCheckbox`/`.Tooltip`/`UIWidgets::Button` 标签只需加表。
- 直接 `ImGui::` 调用需调用点包裹 `SohGui::L(...)`，本轮包裹 6 处：`SohInputEditorWindow.cpp` 的 `ImGui::Button("Close")`、三个 `ImGui::BulletText("Add rumble/LED/gyro device")`；`InputViewer.cpp:681` `ImGui::CollapsingHeader("Analog Angle Values")`；`randomizer_entrance_tracker.cpp:1061` `ImGui::TextWrapped("%d Undiscovered", ...)`（保留 `%d`）。两文件均已 include `SohGui.hpp`。
- 向 `gChineseTable` 追加约 17 条：上述 6 个包裹串 + `Auto scroll`/`Align count to left side`/`Apply & Close` + 8 条多行 tooltip（LED 颜色源、鼠标控制、入口追踪器/物品追踪器/检查追踪器等，键为完整拼接串含 `\n`/`%`）。
- 重跑扫描：过滤后仅剩 3 个纯格式/坐标串（` (%s)`、`%s\u00A0-> %s`、`X:%3d, Y:%3d`），无需翻译。`read_lints` 对 `Localization.cpp` 无报错。临时输出文件已删（保留 `_scan_missing.ps1` 复用脚本）。**未自动编译**（遵守规则）。

## 续 8：VPN 代理 + 子模块下载 + 编译（用户下令"链接上我的vpn走代理，然后下载需要的文件，再编译"）
- 环境已具备：VS2022 BuildTools 17.14.35 + MSVC v143（`C:\Program Files (x86)\Microsoft Visual Studio\2022\BuildTools`，`cl` 不在 PATH，须 `call vcvars64.bat`）。VPN 代理 `http://127.0.0.1:7897` 可用，GitHub 可达。
- 克隆缺失子模块：`git clone --depth 1 https://github.com/harbourmasters/ZAPDTR` 与 `.../OTRExporter`（原 `ZAPDTR`/`OTRExporter` 目录为空，导致 cmake configure 失败）。vcpkg 依赖已缓存在 `build/x64/vcpkg`。
- 改用 **Ninja** 生成器（`build/ninja`），因 VS 生成器 msbuild 启动开销大、5 分钟命令超时内几乎无产出。封装 bat：`_ninja_configure.bat`/`_ninja_build.bat`/`_ninja_single.bat`（注入 vcvars + `PATH` 加 `ninja_bin` + `VCPKG_ROOT=E:\SOHCN\build\x64\vcpkg`）。`ninja -k0` 增量续编 + 单目标重建规避并行争用。
- **修复翻译引入的 2 个编译错误**：
  1. `Localization.cpp:1976` 行首误入字面 `\n`（非法转义序列 C2017 / `n` 未声明 C2065）→ 删除行首 `\n`。
  2. `Menu.cpp:798` `options2.tooltip = SohGui::L("Reset")` 后接平台条件字符串字面量（函数调用后不能直接跟裸字符串，C2143/C2440）→ 改为局部 `std::string tooltip = std::string(SohGui::L("Reset")) + " (Ctrl+R)"`，再 `options2.tooltip = tooltip.c_str()`（字段为 `const char*`）。
- **编译成功**：`[2/2] Linking CXX executable E:\SOHCN\x64\Debug\soh.exe`（123,776,000 字节，`BUILD_EXIT=0`）。
- 临时文件清理：`_ninja_build_k0.bat`、`ninja_build_k0.log` 已删；保留 ninja 包装 bat 与 `ninja_bin`（便于后续增量编译）。
- 注：本环境**可**编译（此前 MEMORY.md 旧结论"无法编译"已更正）。

## 续 9：修复"选中文后全变成 ?"的字体问题
- 现象：选中文后菜单文字全为 `?`。先验证二进制（`soh.exe` 偏移 85251400 处确有正确 UTF-8 字节 `E9 87 8D E7 BD AE` = "重置"），排除编码问题 → 确认为 **ImGui 菜单字体不含中文字形**，每个多字节 UTF-8 被当成单字节映射到缺失字形显示成 `?`。
- 根因：soh 菜单字体只加载了 `Montserrat`（拉丁）+ `NotoSansJP`（日文，glyph range 仅日文）。没有任何中文字体，且没有把 CJK 字形并入菜单字体。
- 修复（不切换字体、最稳健）：在 `soh/soh/OTRGlobals.cpp` 的 `CreateFontWithSize` 中，图标合并之后、以 `MergeMode=true` 把 `NotoSansJP-Regular.ttf` 的 CJK 字形（自定义 range：0x0020–0x00FF / 0x3000–0x303F / 0x4E00–0x9FFF / 0xFF00–0xFFEF，覆盖 `，。：（）！？、～·` 等）并入该 ImFont。ImGui `AddFont` 的 MergeMode 是把字形并入 `Fonts.back()`（即主字体），故能正确烘焙进 Montserrat。
- 为三个标准菜单字体开启 `mergeCJK=true`：`fontStandard(16)`/`fontStandardLarger(20)`/`fontStandardLargest(24)`（覆盖正文 FontDefault 与 header PushFont）。mono/japanese 不需要。
- `OTRGlobals.h`：`CreateFontWithSize` 增第 4 参 `bool mergeCJK=false`；并新增成员 `ImFont* fontChinese`（本方案实际未直接使用，可忽略/留作扩展）。
- 重新编译并链接 `soh.exe` 成功（仅因改了 `OTRGlobals.h` 成员布局触发 soh 全部 205 个 .obj 重编，约两轮 5 分钟增量编译完成）。
- 验证：未能在本环境实际运行 GUI 目视确认（无显示），依据 ImGui 合并机制推断应已修复；如需可让用户运行 `x64/Debug/soh.exe` 选中文验证。

## 续 10：改用用户提供的 DroidSansFallback.ttf 修复中文 ?（用户下令"在Debug文件夹放了ttf文件，请用这个文件"）
- 之前用资源包里的 `NotoSansJP-Regular.ttf` 合并 CJK 字形，但 Noto Sans JP 通常是日文子集（joyo kanji），不含大量中文汉字 → 选中文后仍有大量 `?`。
- 用户把 `DroidSansFallback.ttf`（3.45MB，Android 全 CJK 回退字体）放进 `E:\SOHCN\x64\Debug\`（与 soh.exe 同目录）。
- 修改 `soh/soh/OTRGlobals.cpp` 的 `CreateFontWithSize` 的 mergeCJK 分支：改为用 `Ship::Context::GetPathRelativeToAppDirectory("DroidSansFallback.ttf")` 取路径，经 `std::filesystem::exists` 校验后用 `AddFontFromFileTTF(..., MergeMode=true, 自定义CJK range)` 合并；文件缺失时回退到原 NotoSansJP 资源，再缺失则 `SPDLOG_WARN`。
- CJK 字形范围（自定义）：`0x0020-0x00FF` / `0x3000-0x303F` / `0x4E00-0x9FFF` / `0xFF00-0xFFEF`，覆盖汉字、CJK 标点及全角标点。
- 重新编译并链接 `soh.exe` 成功（`BUILD_EXIT=0`，仅 relink，约 123.8MB）。注意：若用户以后移动 exe，需把 `DroidSansFallback.ttf` 一并带走（按 exe 目录相对查找）。

## 续 11：补翻译 + 修复零星 ?
- 用户报「翻译不全」并「时不时显示 ?」。根因二分：
  1. **零星 ?**：字形范围只到 `0x4E00-0x9FFF`/全角，漏掉 General Punctuation(0x2000-0x206F：— ‘’ “” … •) 与 Arrows(0x2190-0x21FF)。用户贴的 `（选择？关闭？以禁用。` 里 `?` 正是弯引号 “” (U+201C/201D) 没渲染。已在 `OTRGlobals.cpp` 的 `sChineseGlyphRanges` 增加这两段。
  2. **翻译不全**：`ResolutionEditor.cpp` 的 `ResolutionCustomWidget` 直接用 `ImGui/UIWidgets` 调用（不经 `MenuDrawItem` 的 `SohGui::L()`），故这些标签未翻译。已在该文件 `#include "soh/SohGui/Localization.h"` 并对所有直接调用的标签包 `SohGui::L()`（Set fixed vertical resolution / Pixel Count Presets + 其选项 / Horiz.&Vertical Pixel Count / Integer Scaling Settings / Pixel Perfect Mode / Integer scale factor / Automatically scale image... / Additional Settings / Show a horizontal... / Prevent integer scaling... / Allow integer scale factor... / Click to resolve / Click to reenable... / Click to reset... / 滚动条提示）。
- 动态 `{}` 文本（`Viewport dimensions: {} x {}`、`Internal resolution: {} x {}`、`Integer scale factor: {}`）：`MenuDrawItem` 在 `preFunc` 之后对 `widget.name` 调 `L()`，而 `preFunc` 已把 name 格式化成带数字的串 → 模板键匹配不上。修复：在 `PreFunc` 里先 `fmt::format(fmt::runtime(SohGui::L("模板")), ...)`（必须用 `fmt::runtime`，否则 `const char*` 格式串不是编译期常量报 C7595）。
- 带图标的警告/提示串（`ICON_FA_* " ..."`）：`Localization.cpp` 加 `#include "ship/window/gui/IconsFontAwesome4.h"`，表项键与值都保留图标宏；AddWidget 的 WIDGET_TEXT/WIDGET_BUTTON 经 `MenuDrawItem` 的 `displayName=L(name)` 自动翻译，无需改调用点。
- 新增表项（续 10 之后）：Aspect Ratio/Off/Custom/(Select "Off" to disable.)/各 Pixel Count 预设标签/各按钮与提示等共约 30 条。
- 重新编译链接 `soh.exe` 成功（`BUILD_EXIT=0`）。

## 续 12：用户下令"编译"——全量重编验证（BUILD_REMOTE_CONTROL=ON）
- 用户发"编译"指令（即解除"没我命令不能构建"约束，开始构建）。
- 因 `_ninja_configure.bat` 已固化 `-DBUILD_REMOTE_CONTROL=ON`（上次会话为修复 Network features 不可用而开启），本次 `ninja -C build/ninja` 触发近乎全量重编：共 **1287 个目标**（libultraship + soh）。约 15 分钟完成（前段重型 TU 较慢，后段提速至 ~34 目标/分）。
- 结果：`[1287/1287] Linking CXX executable E:\SOHCN\x64\Debug\soh.exe; Copying asset xmls...`，stderr 为空，无 `FAILED`/`error C`/`error LNK`。`soh.exe` 生成时间 2026-07-12 19:02:22。
- 本次编入的关键改动（此前均未编译验证）：① `SohGui.cpp` 全部 `AddGuiWindow` 标题还原英文原串（修复点开后空白的根因——标题是 map 键，不能包 `L()`）；② `SohInputEditorWindow.cpp` 控制器绑定窗口静态文本全面包 `SohGui::L()` 修复"一堆问号+翻译不全"；③ `Localization.cpp` 补 Input Editor 段键（Keyboard/Mouse/Gamepad (SDL)/Link (P1)/Ivan (P2)/Port %d/Set Defaults 等）并删除误译条目 `{ "???", "？？？" }`。
- 监控方式：构建在后台 `Start-Process` 跑，日志写入 `ninja_build_run.log`（30MB，含 `/showIncludes` 噪声）；用 `Get-Content -Encoding ASCII | Select-String '\[\d+/1287\]' | Select-Object -Last 1` 取最后进度。注意 `Get-Content` 必须带 `-Encoding`（否则被安全策略拦截）。

## 续 13：网络(Anchor)菜单翻译
- 用户报"网络部分没翻译"，列出 Connection Settings / Usage Instructions / Host & Port / Name & Color / Room ID / Team ID (Items & Flags Shared) / Restore Defaults / Global Room / Enable / Current Room / Connecting... / Request Team State / Room Settings (Admin Only) / Toggle Anchor Room Window / Clear All Team State / PvP Mode: / Show Locations For: / Allow Teleporting To: / Sync Items & Flags 及 6 条 Usage Instructions 编号说明 + Note 段落。
- 根因：这些文本在 `soh/soh/Network/Anchor/Menu.cpp`，几乎全是**直接 `ImGui::Text/TextWrapped/SeparatorText` 调用**（不经 `MenuDrawItem` 自动翻译），且 `Anchor/Menu.cpp` 此前未 include `Localization.h`（从未用过 `L()`）。
- 修复：`Anchor/Menu.cpp` 显式 `#include "soh/SohGui/Localization.h"`；对全部直接 `ImGui` 文本调用就地 `SohGui::L(...)` 包裹（含 6 条多行拼接串与 `%s Connected`）；对**独立 `UIWidgets::Tooltip(text)`**（UIWidgets.cpp:57 不套 L()）与 **`UIWidgets::WindowButton(label,...)`**（UIWidgets.cpp:189 直接用 label 不套 L()）也在调用点包裹 `L()`。
- `Localization.cpp` 末尾新增「Network / Anchor」分区：直接文本串全量键 + Button/WindowButton/Tooltip/Combobox/Checkbox 标签键（Enable/Disable/Restore Defaults/Global Room/Request Team State/Clear All Team State/Toggle Anchor Room Window/两 Tooltip/PvP Mode:/Show Locations For:/Allow Teleporting To:/Sync Items & Flags）+ 下拉选项值（On + Friendly Fire/Team Only；Off/On/None/All 已存在）。`anchor.hm64.org`/`43383`/`default` 是 CVar 默认值（数据），不译。
- 验证机制：Button/Checkbox/Combobox 标签与 `options.tooltip` 在 UIWidgets 内已套 `L()`（Button:174/Checkbox:365/Combobox 模板:746,750,830,834），故它们只需加表；唯独立 `Tooltip()` 与 `WindowButton()` 不套，必须调用点包裹。
- lint 通过（0 错误）。**未自动编译**（用户本回合未发"编译"指令，遵守"没我命令不能构建"）。

## 续 14：Controls 菜单全部显示 ?? 的根因（**字体未合并 CJK**，非翻译表）
- 用户报"controls 部分显示全是 ??"，列出 Link (P1)/Ivan (P2)/各按钮/Analog Stick Options/Notch Snap Angle:/LED 等均成 ??。
- **根因不是翻译表**：`Localization.cpp` 中 `Link (P1)`→`林克 (P1)`、`Notch Snap Angle:`→`凹槽吸附角度：` 等条目本就正确。真正原因是 **ImGui 字体 atlas 里没有中文字形**。
- 机制（`soh/soh/OTRGlobals.cpp` `CreateFontWithSize`）：`mergeCJK=true` 时才把 `DroidSansFallback.ttf`（或 NotoSansJP 回退）的中文 glyph 合并进该 ImFont。此前 `fontStandard*` 用 `mergeCJK=true`，但**全部 mono 字体**（`fontMonoSmall/Mono/MonoLarger/MonoLargest`，lines 313-316）创建时**未传 `mergeCJK`**（默认 false）。Controls 窗口 `SohInputEditorWindow::DrawElement` 用 `fontMonoLarger`（line 1834），故其中文全部渲染为缺失字形（??/豆腐块）。
- 修复：`OTRGlobals.cpp` 的 4 个 mono 字体 `CreateFontWithSize` 调用均补 `, false, true`（开启 mergeCJK）。这样所有用 mono 字体的窗口（Controls/Presets/gameplaystats/调试器/物品追踪器/主菜单/控制台）都能正确显示中文，杜绝 ??。
- 顺带补齐 Controls 窗口残留未包裹的英文串（之前只显示英文，非 ??）：`Edit axis threshold` 提示、`Notch Snap Angle:` 标签（两者表已有条目）、以及 `Set Defaults for Keyboard` / `Set Defaults for Gamepad (SDL)` 弹窗标题（**OpenPopup 与 BeginPopupModal 必须同步包裹 `L()`** 否则弹窗打不开）——已在 `Localization.cpp` Input Editor 段新增这两条。
- lint 0 错误。**未自动编译**（等用户发"编译"）。
- 经验：此类"整块 ??"几乎必是某字体未 mergeCJK，而非翻译表缺失（表缺失时 `L()` 返回英文原文，不会变 ??）。

## 续 15：分辨率编辑器「Prevent integer scaling…」tooltip 译文未生效（查表键不匹配）
- 用户报要"查找并翻译"：`Prevents integer scaling factor from exceeding screen bounds.\n\nEnabled: ... \nDisabled: ... \n Please note that exceeding screen bounds may show a scroll bar on-screen.`
- 该串是 `soh/soh/SohGui/ResolutionEditor.cpp:303` `CVarCheckbox(...).Tooltip(...)` 的 tooltip。其译文早已存在于 `Localization.cpp:3679-3680`（`防止整数缩放因子超出屏幕边界。…`），本就正确。
- **真正 bug**：所有 UIWidgets 的 tooltip 在显示时会经 `SohGui::L(options.tooltip)` 翻译（UIWidgets.cpp:181/372/602/733/861/1045）。但源码把 `ICON_FA_INFO_CIRCLE` 图标插在字符串**中间**，导致实际传入的是 `...overscan.\n\n Please note...`（一个空格），而表键是 `...overscan.\n\n  Please note...`（**两个空格**）→ 查表失败、回退英文，译文一直没显示。
- 修复：仅改 `ResolutionEditor.cpp` 一处——把图标移到最前（`std::string(ICON_FA_INFO_CIRCLE "  ")` 拼接），并让 `SohGui::L()` 的查表文本与表键逐字一致（保留两个空格）。表无需改动。lint 0 错误。**未自动编译**（等用户发"编译"）。
- 经验：tooltip 等经 `SohGui::L(options.xxx)` 翻译的串，传入的英文**必须逐字符匹配** `gChineseTable` 键；**内联图标（`ICON_FA_*`）插在中间会破坏匹配**——应把图标移到串首（或拆分 L() 调用）。详见续 11 的图标串处理约定。
